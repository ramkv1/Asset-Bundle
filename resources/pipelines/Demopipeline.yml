resources:
  pipelines:
    pipelinedemopipeline:
      name: demopipeline
      libraries:
        - glob:
            include: ../../src/pipelines/DemoPipeline/transformations/**
      schema: default
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${va.catalog_name}
      serverless: true
      root_path:  ../../src/pipelines/DemoPipeline/transformations/**
